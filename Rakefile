# -*- ruby -*-
# frozen_string_literal: true

require 'rake/deveiate'


Rake::DevEiate.setup( 'thingfish-processor-mp3' ) do |project|
	project.publish_to = 'deveiate:/usr/local/www/public/code'
end

